-- Consultas básicas

-- 1. Muestra todos los registros en la tabla "Cliente"
SELECT * FROM ClientePLSQL;

-- 2. Muestra todos los registros en la tabla "Auto"
SELECT * FROM AutoPLSQL;

-- 3. Muestra todos los registros en la tabla "Alquiler"
SELECT * FROM AlquilerPLSQL;

-- 4. Muestra todas los registros en la tabla "Sucursal"
SELECT * FROM SucursalPLSQL;

-- 5. Muestra todas los registros en la tabla "Reserva"
SELECT * FROM ReservaPLSQL;
